#include <stdio.h>

int main()
{
    int n = 9283;

    // scanf("%i", &n);
    // int arr[n], copy[n];
    // for (int i = 0; i < n; i++)
    // {
    //     scanf("%i", &copy[i]);
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     if (i == 0)
    //     {
    //         arr[i] = copy[i] * copy[i + 1];
    //     }
    //     else if (i > 0 && i < n)
    //     {
    //         arr[i] = copy[i + 1] * copy[i - 1];
    //     }
    //     else
    //     {
    //         arr[n - 1] = copy[n - 1] * copy[n - 2];
    //     }
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     printf("%i ", arr[i]);
    // }

    for (int i = 0; i < n; i++)
    {
        printf("%i", n[i]);
    }
    // printf("%i", copy[n-1]*copy[n-2]);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
